#include <stdio.h>

/*Cuenta los caracteres de entrada 2da versión*/

int main(void)
{
    double nc;
    
    for (nc = 0.0; getchar() != EOF; ++nc)
    {
        ;
    }
    
    printf("%.0f\n",nc);
    
    return 0;
}